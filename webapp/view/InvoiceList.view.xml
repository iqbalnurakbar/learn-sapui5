<mvc:View
    controllerName="com.iqbal.app.controller.InvoiceList"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
>
    <Panel
        headerText="Data"
        class="sapUiResponsiveMargin"
        width="auto"
    >
        <content>
            <Button
                text="Load Local Data"
                press=".onLoadLocal"
                class="sapUiSmallMarginEnd"
            />
            <Button
                text="Load Remote Data"
                press=".onLoadRemote"
            />
        </content>
    </Panel>

    <List
        id="invoiceList"
        headerText="{i18n>invoiceListTitle}"
        class="sapUiResponsiveMargin"
        width="auto"
        items="{
         path : 'invoice>/Invoices',
         sorter :[ 
            {
                path : 'ShipperName',
                group : true
            },
            {
                path: 'ProductName',
                descending : true
            },
            {
                path: 'ExtendedPrice',
                descending : true
            }
         ]
        }"
    >
        <headerToolbar>
            <Toolbar>
                <Title text="{i18n>invoiceListTitle}" />
                <ToolbarSpacer />
                <SearchField
                    id="SearchField"
                    width="50%"
                    search=".onFilterInvoice"
                />
            </Toolbar>
        </headerToolbar>
        <items>
            <ObjectListItem
                core:require="{
                    Currency: 'sap/ui/model/type/Currency'
                }"
                title="{invoice>Quantity} x {invoice>ProductName}"
                number="{
                    parts: [
                        'invoice>ExtendedPrice',
                        'view>/currency'
                    ],
                    type: 'Currency',
                    formatOptions:{
                        showMeasure: false
                    }
                }"
                numberUnit="{view>/currency}"
                numberState="{=${invoice>ExtendedPrice} > 50 ? 'Error' : 'Success'}"
                type="Navigation"
                press=".onPress"
            >
                <firstStatus>
                    <ObjectStatus
                        core:require="{
                            Formatter: 'com/iqbal/app/model/formatter'
                        }"
                        text="{
                            path: 'invoice>Status',
                            formatter: 'Formatter.statusText.bind($controller)'
                        }"
                    />
                </firstStatus>
            </ObjectListItem>
        </items>
    </List>
</mvc:View>
